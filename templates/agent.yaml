runtime:
  command: {{AGENT_TYPE}}

tools:
  apt: []
  mise: []
  additional-paths: []
  shell: []

code-server:
  extensions: []
  # Optional. If omitted, bootstrap uses .nvt-agent/code-server/settings.json when present.
  # settings-file: .nvt-agent/code-server/settings.json

plugins: []
# Example:
# plugins:
#   - name: git-credentials
#     source: builtin
#     when: before-agent
#     config:
#       credentials:
#         - match: https://github.com/example/
#           type: token-env
#           token-env: GIT_TOKEN
#           username: x-access-token
#   - name: checkout-repos
#     source: builtin
#     when: before-agent
#     restart: never
#     config:
#       repos:
#         - url: https://github.com/example/public-repo.git
#         - url: https://github.com/example/other-public-repo.git
#           path: vendor/other-public-repo
#           upstream: https://github.com/original-org/other-public-repo.git
#   - name: custom-plugin
#     source: custom
#     command: /custom-plugins/custom-plugin
#     when: after-agent
#     restart: always
#     health:
#       readiness: true
#       # Optional. If omitted, readiness is based on plugin lifecycle state.
#       # command: /custom-plugins/custom-plugin --ready
#     config: {}
